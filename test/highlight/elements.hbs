<foo
 {{! <- tag }}
  string-attr="string"
  {{! ^ attribute }}
  {{!        ^ operator }}
  {{!         ^ string }}
  number-attr=12
  {{!         ^ number }}
  mustache-attr={{value}}
  {{!             ^ variable }}
  concat-attr="string {{mustache}}"
  {{!          ^ string }}
  {{!                 ^ tag.delimiter }}
  {{!                   ^ variable }}
  class="foo {{if condition "bar"}}"
  {{!          ^ keyword.conditional }}
  {{!             ^ variable }}
  {{!                       ^ string }}
  ...attributes
  {{! ^ attribute }}
></foo>
